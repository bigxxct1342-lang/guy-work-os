# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local backups
*.backup.json
GUY_*_BACKUP_*.json

# Seeded design-canvas output — 2.5MB, regenerated from the .dc.html sources
.design/*.html
!.design/*.dc.html

# Working files for the design-review canvas (published as an Artifact, not app code)
.design/
