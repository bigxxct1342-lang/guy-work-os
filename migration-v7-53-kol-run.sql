-- PORKCHOP G · V7.53 — ตัวเลขจริงของรอบ KOL
--
-- รอบหนึ่งมี KOL ได้ถึง 50-60 คน และ Agency ทำตารางรายคนมาให้อยู่แล้ว
-- แอปนี้จึงไม่เก็บรายชื่อ เก็บแค่ "กี่คนอยู่ด่านไหน" ของทั้งรอบ
-- ทุกอย่างอยู่ใน jsonb ก้อนเดียว จะเพิ่มตัวนับใหม่ทีหลังก็ไม่ต้อง migrate อีก
--
-- รูปร่างของ run:
--   { "n": 52,
--     "tier":  { "mega":5, "macro":7, "micro":20, "nano":20 },
--
--     -- Storyline กับ Draft เป็นลูปเดียวกัน ใช้ชุดหัวข้อเดียวกัน
--     -- สี่ช่องนี้คือ "ตอนนี้แต่ละคนอยู่ตรงไหน" ไม่ทับกัน รวมแล้วต้องเท่ากับ n
--     "sl":    { "ok":40, "fix":8, "resub":5, "drop":4, "round":2,
--                "why":["slow","bad"], "note":"2 คนหายไปเลย" },
--     "dr":    { "ok":30, "fix":15, "resub":7, "drop":0, "round":2, "why":[], "note":"" },
--
--     "posts": [ { "d":"2026-10-01", "n":7 } ],          -- วันไหนลงกี่คน
--     "boost": [ { "from":"2026-10-01", "to":"2026-10-14", "n":20 } ],
--     "report": true }

alter table public.kol_campaigns
  add column if not exists run jsonb not null default '{}'::jsonb;
